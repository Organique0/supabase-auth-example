import AuthButton from "@/components/AuthButton";

const SetupPage = async () => {
    return (
        <div>
            create a server
            <AuthButton />
        </div>
    );
}

export default SetupPage;